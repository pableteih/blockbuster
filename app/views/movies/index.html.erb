<p style="color: green"><%= notice %></p>

<%= link_to "Agregar una nueva pelicula", new_movie_path, class: "btn btn-outline-secondary" %><br/><br/>

<h1>Peliculas</h1>

<div id="movies" class="row">
  <% @movies.each_slice(3) do |movie_slice| %>
    <div class="row">
      <% movie_slice.each do |movie| %>
        <div class="col-md-4">
          <div class="movie-card">
            <%= render movie %>
            <p>
              <%= link_to "Ir a esta película", movie, class: "btn btn-outline-secondary" %>
            </p>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>
</div>

<div class="d-flex justify-content-center">
  <nav aria-label="Page navigation">
    <ul class="pagination">
      <%== pagy_bootstrap_nav(@pagy) %>
    </ul>
  </nav>
</div>