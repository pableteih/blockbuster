<p style="color: green"><%= notice %></p>

<%= render @movie %>

<div>
  <%= link_to "Editar esta película", edit_movie_path(@movie), class:"btn btn-outline-secondary" %> |
  <%= link_to "Volver a películas", movies_path, class:"btn btn-outline-secondary" %>

  <%= button_to "Eliminar esta pelicula", @movie, method: :delete, class:"btn btn-outline-secondary" %>
</div>
